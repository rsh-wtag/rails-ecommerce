<h1>Your Cart</h1>
<% if @cart.cart_items.any? %>
  <table class="table">
    <thead>
      <tr>
        <th>Product</th>
        <th>Quantity</th>
        <th>Price</th>
        <th>Total</th>
        <th>Actions</th>
      </tr>
    </thead>
    <tbody>
      <% @cart.cart_items.each do |item| %>
        <tr>
          <td><%= item.product.name %></td>
          <td><%= item.quantity %></td>
          <td><%= number_to_currency(item.product.price) %></td>
          <td><%= number_to_currency(item.product.price * item.quantity) %></td>
          <td>
            <%= link_to 'Edit', edit_cart_cart_item_path(@cart, item), class: 'btn btn-sm btn-warning' %>
            <%= button_to 'Remove', cart_cart_item_path(@cart, item), method: :delete, data: { confirm: 'Are you sure?' }, class: 'btn btn-sm btn-danger' %>
          </td>
        </tr>
      <% end %>
    </tbody>
    <tfoot>
      <tr>
        <td colspan="3"><strong>Total:</strong></td>
        <td><strong><%= number_to_currency(@cart.cart_items.sum { |item| item.product.price * item.quantity }) %></strong></td>
        <td></td>
      </tr>
    </tfoot>
  </table>
  <%= link_to 'Continue Shopping', products_path, class: 'btn btn-secondary' %>
  <%= button_to 'Checkout', checkout_cart_path(@cart), method: :post, data: { confirm: 'Are you sure you want to checkout?' }, class: 'btn btn-primary' %>
<% else %>
  <p>Your cart is empty.</p>
  <%= link_to 'Continue Shopping', products_path, class: 'btn btn-secondary' %>
<% end %>
